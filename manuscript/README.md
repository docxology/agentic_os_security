# manuscript/

The manuscript for **"Agentic Security and Operating Systems: A Deep Review and Prospectus of OpSec, Cognitive Security, and Agentic Cyber Security in the Emerging Present and Future"** (v0.1.0, private).

## Orientation

18 Pandoc-markdown sections (see `AGENTS.md` for the full inventory with labels), a `references.bib` with exactly 71 keys (65 source-derived + 6 scholarly), a `config.yaml` identity/experiment block, and a deterministic `{{TOKEN}}` hydration pipeline. Rendering to PDF/HTML happens from the sibling template repository; this directory holds the sources and conventions only.

## Section map

| Parts | Sections | What they do |
|---|---|---|
| Framing | 00–03 | Abstract; introduction (agents at the kernel boundary); threat model (exploitation vs authorized misuse); evaluation framework (9 properties over 24 candidates, qualitative stances) |
| Systems review | 04–08 | Qubes OS in depth; NixOS in depth; conventional desktops/hardening; servers & agent-execution infrastructure; boundary comparators & non-Linux |
| Agentic domains | 09–13 | Agentic authority architecture (7 trust domains, 9 controls); cognitive security (authorized-misuse surface); operator OpSec; orchestration security; configuration generation ≠ authorization |
| Horizon | 14–16, 99 | Forecast 2028–2031 with confidence tiers; 8 scenario recommendations; conclusion; generated references |

## Quick rules

- Citations: `[@key]` from `references.bib` only. Cross-refs: `[@sec:...]`/`[@fig:...]`/`[@tbl:...]` only.
- Tokens: `{{TOKEN}}` only from the plan in `docs/syntax_guide.md`; generated by `scripts/z_generate_manuscript_variables.py`.
- No mermaid fences; never write raw LaTeX citation or reference macros in Markdown; no hand-written bibliography.
- Style: RASP + attribution discipline (`docs/style_guide.md`); no numeric security scores.

## Commands

```bash
uv run python scripts/10_evaluation_analysis.py           # figures + data artifacts (must precede variable generation)
uv run python scripts/z_generate_manuscript_variables.py  # resolve {{TOKENS}}
uv run pytest tests/ --cov=src --cov-fail-under=90        # structure/token/parity checks
```
