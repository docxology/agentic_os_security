# .agents/ — agent-facing entry points

Agent-facing configuration for working on `agentic_os_security` (private deep review + prospectus: agentic security & OS security — OpSec, cognitive security, agentic cyber security).

- `skills/agentic-os-security/` — the project skill: load `SKILL.md` before operating the matrix, evidence registry, figures, or manuscript pipeline.
- Root operating contract: `AGENTS.md` at the repo root; deep rules per directory (`scripts/AGENTS.md`, `src/AGENTS.md`, `manuscript/AGENTS.md`, `data/AGENTS.md`).
